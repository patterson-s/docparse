# Copy this file to .env and fill in your key, OR pass --api-key on the CLI,
# OR set MISTRAL_API_KEY as a system environment variable.
#
# The Streamlit UI always prompts for the key in the sidebar — no .env needed there.

MISTRAL_API_KEY=
